package com.example.Marketplace;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

public class ProjectTest {
    @Test
    public void testCICDPipelineSetUp() {
        // this test case is only to evaluate if the pipeline works when pushing this test case to the repo the build should
        // succeed
        assertTrue(true);
    }
}
